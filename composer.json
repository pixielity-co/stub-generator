{
    "$schema": "https://getcomposer.org/schema.json",
    "name": "pixielity/stub-generator",
    "description": "A powerful, fluent stub template processor for PHP with support for placeholders and optional sections",
    "type": "library",
    "keywords": [
        "stub",
        "template",
        "generator",
        "placeholder",
        "scaffolding",
        "code-generation",
        "php"
    ],
    "homepage": "https://github.com/pixielity-co/stub-generator",
    "license": "MIT",
    "authors": [
        {
            "name": "Pixielity Team",
            "email": "team@pixielity.co"
        }
    ],
    "support": {
        "issues": "https://github.com/pixielity-co/stub-generator/issues",
        "source": "https://github.com/pixielity-co/stub-generator",
        "docs": "https://github.com/pixielity-co/stub-generator#readme"
    },
    "require": {
        "php": "^8.4"
    },
    "require-dev": {
        "phpstan/phpstan": "^2.0",
        "phpstan/phpstan-strict-rules": "^2.0",
        "phpstan/extension-installer": "^1.4",
        "laravel/pint": "^1.18",
        "rector/rector": "^2.0",
        "phpunit/phpunit": "^11.5",
        "mockery/mockery": "^1.6"
    },
    "autoload": {
        "psr-4": {
            "Pixielity\\StubGenerator\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Pixielity\\StubGenerator\\Tests\\": "tests/"
        }
    },
    "config": {
        "sort-packages": true,
        "optimize-autoloader": true,
        "preferred-install": "dist",
        "allow-plugins": {
            "phpstan/extension-installer": true
        },
        "platform": {
            "php": "8.4.0"
        }
    },
    "scripts": {
        "test": "phpunit || ([ $? -eq 1 ] && echo 'Tests passed with warnings' && exit 0 || exit $?)",
        "test:coverage": "phpunit --coverage-html=build/coverage",
        "lint": "pint --test",
        "format": "pint",
        "typecheck": "phpstan analyse --memory-limit=2G",
        "refactor": "rector process",
        "refactor:dry": "rector process --dry-run",
        "check": [
            "@test",
            "@lint",
            "@typecheck",
            "@refactor:dry"
        ],
        "fix": [
            "@format",
            "@refactor"
        ],
        "post-install-cmd": [
            "@php -r \"file_exists('build') || mkdir('build', 0755, true);\""
        ],
        "post-update-cmd": [
            "@php -r \"file_exists('build') || mkdir('build', 0755, true);\""
        ]
    },
    "scripts-descriptions": {
        "test": "Run PHPUnit test suite",
        "test:coverage": "Run tests with HTML coverage report",
        "lint": "Check code style with Laravel Pint",
        "format": "Fix code style with Laravel Pint",
        "typecheck": "Run PHPStan static analysis",
        "refactor": "Apply Rector refactoring changes",
        "refactor:dry": "Preview Rector refactoring changes",
        "check": "Run all quality checks (test, lint, typecheck, refactor)",
        "fix": "Fix all auto-fixable issues (format, refactor)"
    },
    "minimum-stability": "stable",
    "prefer-stable": true
}
